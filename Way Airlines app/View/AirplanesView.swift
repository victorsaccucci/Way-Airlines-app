import SwiftUI

struct AirplanesView: View {
    var body: some View {
        VStack {
            Text("Settings")
                .font(.largeTitle)
        }
        .padding()
    }
}
#Preview {
    AirplanesView()
}
